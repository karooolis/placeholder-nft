{
  "name": "placeholder-nft",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "private": true,
  "workspaces": [
    "frontend",
    "contracts"
  ],
  "scripts": {
    "start": "yarn --cwd frontned start",
    "build": "yarn --cwd frontend build",
    "dev": "yarn --cwd frontend dev",
    "lint": "yarn --cwd frontend lint"
  }
}
