{
  "name": "placeholder-nft",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "private": true,
  "workspaces": [
    "frontend",
    "contracts"
  ],
  "scripts": {
    "start": "yarn --cwd frontend start",
    "build": "yarn --cwd frontend build",
    "dev": "yarn --cwd frontend dev",
    "lint": "yarn --cwd frontend lint",
    "test:contracts": "yarn --cwd contracts test",
    "deploy:contracts": "yarn --cwd contracts deploy",
    "verify:contracts": "yarn --cwd contracts verify"
  }
}
